# Exercise Log

A single-file web app for logging physio sessions and walks. No server, no
accounts, no network calls: records live in the browser's own storage, and
Export/Import CSV moves them between devices.

Open `dashboard.html` to use it. On iPhone or iPad it must be opened from a
web address — a file opened from Files or iCloud Drive lands in a preview
that never runs JavaScript.
